const utils = require("../js/utils.js")

const admin = {

  newLocation: (name = "none", desc = "none", exits = {}) => {

  },

  editLocation: () => {},

  banUser: (user) => {},

  teleport: (x, y, z) => {},

}

module.exports = admin
